version: '3'

services:
    app-build:
        build: 
            context: ./app-build
            dockerfile: Dockerfile
        volumes:
            - ./pwa-app:/pwa-app
    nginx:
        image: nginx:1.13.6
        ports:
            - 80:80
